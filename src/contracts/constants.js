/**
 * Solana Program Configuration
 * Update these values when deploying your smart contract.
 */

export const SOLANA_CONFIG = {
  // Your deployed Program ID
  PROGRAM_ID: "YOUR_SOLANA_PROGRAM_ID_HERE",
  
  // RPC Endpoint (Mainnet/Devnet)
  RPC_URL: "https://api.mainnet-beta.solana.com",
  
  // Presale Vault Address (where funds go)
  VAULT_ADDRESS: "YOUR_VAULT_PUBLIC_KEY_HERE",
  
  // $PWIFE Token Mint Address
  TOKEN_MINT: "YOUR_TOKEN_MINT_ADDRESS_HERE",
  
  // Decimals for the token
  TOKEN_DECIMALS: 9
};
